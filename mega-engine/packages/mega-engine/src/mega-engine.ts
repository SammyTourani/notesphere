/**
 * Clean Mega Grammar Engine - Only Uses REAL nlprule WASM
 * This is the simplified, clean version that only uses the sophisticated grammar engine
 */

export { NlpruleRealEngine as MegaEngine } from './mega-engine-nlprule.js'; 